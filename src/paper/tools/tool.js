import { EventDispatcher } from '../../events.js';

class Tool extends EventDispatcher {
  constructor() {
    super();
  }

  render(ctx) {
    
  }

  handleEvent(event) {

  }
}

export default Tool;
